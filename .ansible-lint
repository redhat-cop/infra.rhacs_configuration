---
profile: production
exclude_paths:
  # Sample playbook for developers to test a specific module. This playbook is
  # not supposed to work as it is, but rather be used as an example.
  - tests/sample_manual_test.yml
supported_ansible_also:
  - "2.14"
...
